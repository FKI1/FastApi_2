# FastAPI REST API с аутентификацией и авторизацией

REST API для управления пользователями и объявлениями с системой JWT аутентификации и ролевой моделью доступа.

## 🚀 Основные возможности

- **Аутентификация по JWT токенам** (срок действия 48 часов)
- **Управление пользователями** (CRUD операции)
- **Управление объявлениями** (CRUD операции)
- **Ролевая модель доступа** (неавторизованный пользователь, user, admin)
- **Проверка прав доступа** на уровне эндпоинтов
- **Валидация данных** с помощью Pydantic
- **Автоматическая документация** Swagger UI и ReDoc
- **Docker контейнеризация** для простого развертывания

## 📋 Требования

- Docker 20.10+ и Docker Compose 1.29+
- Или Python 3.11+ (для запуска без Docker)

## 🛠 Установка и запуск

### Способ 1: Запуск с Docker (рекомендуется)

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd FastApi_2